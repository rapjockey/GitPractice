# Day01(learning) Date - 10-06-2026 #Diff Between Git and Github 

# there are 4 stages of git init -> git add -> git commit -> git push 
# git init -> use for initialize repo #git add-> use for track to file (git add . -> tracks all files)
# git commit -> creates a repo and ready for push #git push -> added a repo to github

# Branches In Git #git branch -> gives info about branch 
# git branch -> creates new branch #git switch -> switches one branch to another branch 
# git checkout -> checks the branch also switch
# git merge -> Merges the two branches

# .gitinore -> put the file into this file and files will untrackable for secure or unwanted files # .gitkeep -> git not tracks empty folders there for using this file we force to track folder.

# <---------------------------------------------------------------------------------------------->
